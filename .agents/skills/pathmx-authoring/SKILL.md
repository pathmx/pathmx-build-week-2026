---
name: pathmx-authoring
description: Retired compatibility name for the canonical PathMX skill. Use only when an older prompt explicitly invokes pathmx-authoring; new PathMX work must use pathmx.
---

# Retired PathMX authoring name

This legacy name remains so older Build Week prompts fail gracefully.

For every PathMX task, read and follow the installed canonical
[`pathmx` skill](../pathmx/SKILL.md). Its checked references replace the stale
reference material retained in this legacy directory's Git history.

Do not combine the old and new instructions. Do not add new guidance here.
